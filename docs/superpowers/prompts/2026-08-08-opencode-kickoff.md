# OpenCode implementation kickoff

Read `docs/superpowers/specs/2026-08-08-auto-trader-design.md` and Plans A, B, and C in `docs/superpowers/plans/` before changing code. Implement the autonomous **paper-only** trader using the configured specialist agents and this exact schedule.

1. Inspect `git status --short` and the current tree. Do not discard or overwrite pre-existing user changes. Do not modify `src/botta/`, create live order execution, wallet signing, or a real-funds path.
2. Dispatch `rust-core` alone for Plan A Tasks 1-7. It must use test-first work, run `cargo clippy --all-targets -- -D warnings` and `cargo test` after each task, and commit each completed task. Wait for it to finish. Review its final diff and test results before proceeding.
3. Starting from the accepted core commit, create two isolated git worktrees on dedicated temporary branches: one for `rust-runtime` and one for `dashboard`. Dispatch both as background subagents at the same time. `rust-runtime` implements only Plan A Tasks 8-14; `dashboard` implements only Plan C. Do not let either worker modify files outside its allowed scope. Wait for both to finish, inspect their commits, run their prescribed tests, and integrate one clean commit at a time into the current branch. Remove the temporary worktrees and branches only after successful integration.
4. Dispatch `news-pipe` only after `rust-runtime` is integrated. It implements Plan B, including the `[news]` config change in the integrated `traderd/traderd.toml`. Run `uv run --group dev pytest tests/test_tg_news_pipe.py -q`, inspect the diff, and integrate its commits.
5. Dispatch `risk-auditor` last in read-only mode. It must audit paper-only guarantees, sizing, fill/fee/slippage arithmetic, risk gates, API contracts, secrets, and accidental live-order paths. Address every concrete finding with a focused test-backed commit, then ask the auditor to re-check the final state.
6. Finish by running the complete applicable Rust, Python, and dashboard test/build suites. Return a concise summary with every commit hash, test command/result, remaining manual checks, and any blockers. Never invent a passed test or completed manual verification.
