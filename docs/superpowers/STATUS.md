# peri — LIVING STATUS (read me first, agents)

> Point-in-time truth: **2026-08-27**. **THE 2026-08-26/27 PIVOT (user-locked):**
> the monorepo consolidated into ONE system — **peri**, an autonomous LLM perp
> trader (live-capable, Trench wallet, native + xyz dexes). Everything else
> (kestreld, databoard, dashboard, dash-plumbing, tg_news_pipe) is **retired to
> `archive/`** — reference only, do not extend. `AGENTS.md` and
> `docs/superpowers/specs/2026-08-27-peri-design.md` describe the new system.
> Everything below the peri section is the kestrel-era record, kept for history
> and pattern lineage.

## peri LIVE (2026-08-27 ~11:50Z, user-ordered)

`mode="live"` on Bhanu's explicit word. Ledger reset for the live era
(paper history archived to `peri-paper-era.db`; den+news carried over).
First live book (manual tickets via agent key, adopted+managed by the analyst):
KIOXIA short 0.117@333.76 (SL342/TP315, carry-positive) · SOL short limit
1.12@104.75 (SL105.6/TP102.2) · MRNA short limit 0.27@145.5 (SL148.6/TP137.5).
Entry cap 6/6 keeps peri manage-only until 00:00Z. Boot fixes landed: native+dex
coin map, unified-account abstraction tolerance, adopt-per-cycle.

## peri status (2026-08-27, overnight build session)

| Item | State |
|---|---|
| Rename botta → peri (package, scripts, session) | ✅ `a47fe60` |
| Archive retired systems | ✅ `dcbc5f1` |
| Core build (one brain, frozen gates, dex-aware market, engine, feed, news) | ✅ `94bf113` |
| Reasoning-model hardening (empty-content retry, 16k budget, 120s timeout) | ✅ `365c5a5` |
| Gates | pytest **90/90**, ruff clean |
| Live executor | testnet-verified 2026-08-26 (SOL open+brackets+close, builder fee approved 30 tenths-bp) |
| First real dry cycles | 2 cycles vs live mainnet: view coherent, 2 entries gated+sized (risk exactly 1.5%, RR>2), cycle 2 held without churn |
| Paper book | xyz:CRWD long 3x (208.32, SL 203.5/TP 218.8) · HYPE long 4x (82.31, SL 80.9/TP 85.3) |

**Mainnet go-live (2026-08-27 03:55Z): wallet WIRED.** `.env` now has
`HL_ACCOUNT_ADDRESS=0x762d3B4D…390bA` (Trench wallet; on-chain verified: $8.30
xyz clearinghouse holding the live xyz:NVDA long, builder fee already approved
30 tenths-bp, no API agents yet) + fresh mainnet `HL_AGENT_KEY` minted by
make_agent.py (testnet pair preserved as `HL_TESTNET_*`). **Remaining user
actions:** (1) fund the wallet ($250+ recommended floor); (2) authorize agent
address `0xYOUR_AGENT_ADDRESS` on the account —
app.hyperliquid.xyz → More → API (connect/export the Trench wallet), or
whatever agent flow the Trench app exposes; (3) say go → flip `mode="live"`.
An unauthorized agent fails loudly at HL — no silent path. Also open: rotate
`TG_BOT_TOKEN`; set `[notify] chat_id` for TG alerts.

---

# botta paper-trader — LIVING STATUS (read me first, agents) (system: kestrel)

> Point-in-time truth: **2026-08-09**. Supersedes conflicts in older specs/plans. Spec `docs/superpowers/specs/2026-08-08-auto-trader-design.md` remains foundational, but the AMENDMENTS below are user-locked and normative.

## What's built (all integrated on `master`)

| Track | State | Commits |
|---|---|---|
| Plan A — traderd core (Tasks 1–7) | ✅ | `ce19335…a08b21e` |
| Plan A — runtime (Tasks 8–14) + stub smoke | ✅ | `5d4dab0…4e8e49e` |
| Plan C — dash (Next.js, 4 pages) | ✅ | `0009c81…72517f7` (merge `d67c64a`) |
| Plan B — tg news pipe | ✅ | `99023d2`, `69b9786` |
| Plan D-1 — l2Book impact fills | ✅ | `ed0581a` |
| Plan D-2 — allDexsAssetCtxs stream (+ snapshot fix) | ✅ | `2562e004`, fix `8bd3be0` |
| Plan D-3 — fundingHistory boot seed | ✅ | `8addb79` |
| Plan D-4 — candle ATR in analyst prompt | ✅ | `522e67f` |
| Audits | ✅ | Plan A full audit → fixes `11e233c`,`d0c5875`,`e90b0e7` ; Plan D audit 8/8 PASS ; post-tuning fixes `55cce9b` (snapshot∪position markets), `ee441ee` (zero-mark guards) |
| Dash hardening | ✅ | `ab24c80` (CORS proxy `e82baeb` + null-safe fmt) |
| **Dash v2 — full registry rebuild** | ✅ | spec `ed89c13`, plan `758ae57`, impl `340f06f 1e00b8e 4478afe 5e82317 9495612 d9ecf7a 63c9325` + cleanup (this commit). Registries @bklit/@evilcharts/@kibo-ui/@openstatus vendored + rethemed; live candles via lightweight-charts + HL `candleSnapshot` proxy (`app/api/hl/candles`, read-only public data — PAPER invariant intact; `xyz:` coins map 1:1, verified); entry/SL/TP price lines on the book's hero; tape + ⌘K + NumberFlow + daily-PnL calendar + kill-budget gauge + stack-health blocks. Evolved aesthetic (gradients/glow permitted) is normative — see dash/README "v2 registry rebuild" incl. registry-lessons table. |
| **Dashboard v3 (dashboard/)** | ✅ ACCEPTED | Reference-locked rebuild via Opus workflows (runs `wf_c96519dc-eee` r1, `wf_0315ef95-8e9` r2, `wf_b7dccc2c-386` hotfix; ~2.0M subagent tokens). Spec+amendment `cb20549`/`e14c42b`, code `7fc71ca`→`06f441a`. Dual themes (canonical light = user's own design; warm-charcoal dark), sans discipline, measured-WCAG ink tokens, dot-matrix idiom, lightweight-charts candles (oklch crash fixed via canvas pixel-readback). Serves `127.0.0.1:3474` (`bun run start`). Old dash/ nuked (`3ca37ce`, plumbing in `dash-plumbing/`). Open: light primary-button contrast decision (ink-on-orange vs darker orange), lint debt `lib/ws.ts:18`. |
| **Autonomy hardening (R1/R2/T1/T2)** | ✅ COMPLETE | Plan `fbcb013`. R1 `734d7a6` ABBA-deadlock fix + reconnect + truthful ws_connected + 3s→503. R2 `770ae3b` StaleData gate, watchdog, TG alerts, systemd (supervising). T1 `dc4e17b` reviewer mark/uPnL/R + fee hurdle, ledger-derived churn gates, regime gate, amendment #9. T2 (this commit) `/api/gates` + `/api/analytics` (conviction buckets, veto counterfactuals via candle replay, exit mix), screener exclusion feed, dash /intel Gates+Analytics cards. Tests 147→226. First live data: conviction 0.70–0.75 = 16 closes 38% −$62.72 vs 0.75–0.80 = 4 closes 75% +$41.89; first 3 counterfactuals: vetoes beat brackets by ~$21. |
| **Wave 3 — backtest harness (`kestreld backtest`)** | ✅ B1+B2+B3 | Spec `c94b145`. Subcommand only: it runs and exits before any daemon setup, never opens `kestreld.db`, has no order surface. `fetch` → `backtest_cache.db` (cache-first, resumable, throttled like the funding seed); `run` = 1m replay of the REAL screener → gate chain → sizing → brackets, fills by the counterfactual walker's rules (now shared with `analytics`); `sweep` = repeatable `--sweep key=v1,v2`, cartesian grid on one loaded tape, ranked by net then shallower maxDD. New knob `[sizing] stop_floor_pct` (serde default **1.0** = the locked floor from `53a5a64`, so the daemon sizes exactly as before) makes the stop clamp sweepable. **First real sweeps** `docs/backtests/2026-08-09-first-sweeps/` — window 2026-08-06..08 (271 markets, 4074 bars; **HL serves only ~3.6 days of 1m history**, so the spec's 7-day window is unobtainable, ever): stop floor **1.0 wins** (net −75.78 vs −124.84 @0.6 / −148.02 @1.4); `cooldown_after_sl` **120 > 30** (−75.78 vs −91.31); regime **on ≡ off** — BTC `vol1h` peaked at **0.079%** against the 1.5 ceiling, 0 of 4010 minutes over, so the gate cannot fire in a calm tape; conviction is a **pure size multiplier** mechanically (identical 60 entries / 33.3% win / 1.54 payoff at 0.70/0.75/0.80, net ∝ margin). All runs are capacity-bound: 60 entries = `daily_cap` 20 × 3 days, refusals 3416 DailyCap / 2349 MaxConcurrent / 2142 Paced. Tests 248→311. |
| **Wave 3 batch B4 — capacity/fee sweep axes + `backtest stats`** | ✅ code-complete, gates green | Five new sweep axes on top of B3's four, same pattern (each with an override-plumbing test on the golden fixture, `sweep.rs`): `daily_cap`→`risk.daily_cap`, `max_concurrent`→`risk.max_concurrent`, `min_score`→`screener.min_score` (all three were already `--set`-able, just newly aliased); `tp_mult`→new knob `[sizing] tp_mult` (serde default **2.0** exactly — same treatment B3 gave `stop_floor_pct`, live `tp_pct = tp_mult·stop_pct` replaces the old `2.0·stop_pct` literal, `kestreld.toml` untouched); `margin`→pins **both** `sizing.margin_min` AND `sizing.margin_max` to the swept value (`clamp(x,v,v)=v`), overriding the conviction-driven raw margin entirely — pairs with `daily_cap`/`max_concurrent` to compare fewer-larger vs more-smaller positions at a similar aggregate notional (mapping documented in `sweep.rs`'s `MARGIN_NOTE` + README). New read-only subcommand `kestreld backtest stats --from --to [--markets]` (`stats.rs`, no gates/fills/pnl — walks the same rows the replay scores via a new free fn `engine::rows_at`): BTC + universe-pooled + per-market (top 15 by volume proxy) vol1h% percentiles, funding_z share past `|z|>1/2/2.5`, screener score distribution (raw `screener::screen` pooled with an open floor, bucketed against the real `min_score`), turnover/fee reference (`size_position` at the window's measured median vol1h). Tests 311→321. Cache refreshed same session: `kestreld backtest fetch --from 2026-08-05 --to 2026-08-09` → 340 markets probed, 271 with data, actual window obtained **2026-08-06 04:06 UTC → 2026-08-09 21:38 UTC (3.73 days)**, matching the ~3.6-day retention claim. First real `stats` run on that window (`docs/backtests/2026-08-09-stats-2026-08-06_2026-08-09/`, 5373 bars): **BTC vol1h max 0.0794%** (confirms B3's 0.079% independently) vs the whole universe's pooled max **4.3956%** — other markets do get volatile, BTC alone undersells the tail; funding_z `|z|>2.5` (the screener's side-flip threshold) hits **5.12%** of market-minutes; `min_score=1.8` clears **45.26%** of pooled market-minutes (permissive); round-trip fee confirmed **exactly 15.00bp** of notional at every conviction (linear-in-notional identity, matches amendment #9's estimate). No commits made (per task instructions — lead commits). |
| Gates | `cargo test` **311/311**, clippy `-D warnings` clean, dash `pnpm build` + fixtures build green, pytest 51/51 + tg pipe 3/3 |

## INCIDENT 2026-08-09: traderd ws wedge (resolved by restart, bugs open)

Both HL ws streams died 10:42–10:45Z (`ctxs stream ended` / `hl_ws stream ended`, ws closed) and never reconnected; `/api/positions` + `/api/snapshot` wedged (handlers hang on ws-fed state), `health.ws_connected` kept reporting **true**. Blind ~2h; restarted 12:47Z (pid 341866), all endpoints green. **WEDGE #2 (14:00Z, 72min after restart):** same two endpoints hung again — but the new process had NO ws death; it logged `hl meta Shape("expected top-level array")` REST failures at 12:52Z then wedged. Two different triggers → identical hang ⇒ root cause is a shared-state lock held across a failure path (ws close handler AND REST error path both poison it), not the ws itself. Restarted again 14:0xZ (pid 375662). **Open traderd follow-ups (priority zero):** F-ws0 find+fix the state-lock hold-across-await/error (audit mids/ctxs lock discipline; both observed triggers precede permanent positions/snapshot hangs); **RESOLVED 2026-08-09 evening:** true root cause was an ABBA deadlock (snapshot RwLock × engine Mutex; ws deaths + REST errors were red herrings — DB forensics: equity rows stop 12:30:00Z/13:24:33Z). F-ws0..3 fixed `734d7a6` (lock discipline invariant, jittered reconnect + dead reset-arm fix, freshness-derived ws_connected, 3s→503 handler timeouts, reqwest 10s). F-ws4..6 fixed in R2 (StaleData pre-gate 120s, watchdog + episode-deduped TG alerts, gate refusals now land on decision rows as ` gate_refused:<Kind>`, systemd user unit `deploy/traderd.service` — NOW SUPERVISING, setsid path is fallback only). Tests 147→198 (R1 175, T1 198). Known gap for T1: entry staleness uses snapshot.ts (REST-stamped) — harden with max(snapshot_age, ws_feed_age).

## USER-LOCKED AMENDMENTS (normative over older spec text)

1. **Analyst = muse-only** (`56c200c`): fallback model/endpoint REMOVED entirely; refusal/error/timeout → 2 retries → `decision: None`; `max_completion_tokens = 16000`; per-call timeout 60s. No gpt-5 fallback exists.
2. **Universe floors raised** (`b29d645`, day-1 data): `min_vlm_native = 2,000,000` · `min_vlm_dex = 500,000` (was 500k/200k) — sub-$0.05 chop coins were 6/6 stop-outs.
3. **conviction_min = 0.70** (`b29d645`, was 0.65).
4. **Notify wired**: `TG_BOT_TOKEN` in `.env` + `[notify] chat_id = 123456789` (`c0793aa`); bot `@yourbot_bot`. Bot tokens live only in `.env`; rotate through BotFather if one is ever exposed.
5. **News surface expanded**: 10 tg channels (`cfa376b` + `bf7082d`), 9 RSS feeds (The Block 403'd → blockworks + DLNews swap), Tavily per-nominee with **dedicated key `tvly-dev-…` in `.env`** (previously an old `tvly-prod` from shell env took precedence — dotenvy never overrides process env!).
6. **Day-roll + kill latch**: kill-switch latches entries per-day; resets at 00:00 UTC day roll with fresh day-open equity (`11e233c`). Daily counters keyed by UTC day.
7. **Dash data path**: browser must NOT cross origins — Next rewrites `/traderd/:path*` → `127.0.0.1:7411` (`e82baeb`); traderd never adds CORS headers.
8. **Spec-deviations accepted in code**: features available when span≥5min OR len≥300 (HL ~1/5s mids cadence); Store path `sqlite:{cwd}/traderd.db?mode=rwc`; funding_z uses |raw z| (spec §5 literal, seed+stream+poll share one change-guarded ring of 200).
9. **Churn control + regime + reviewer context** (user-directed 2026-08-09, Phase T1; commit e26f2c4): `[risk] per_market_daily_cap = 3` (entries per market per UTC day, refusal `PerMarketCap`) · `cooldown_after_sl_min = 120` (asymmetric: a market whose last close was an **SL** waits 120m; TP/veto/time-stop keep `cooldown_min = 30`, refusal stays `Cooldown`) · `morning_entry_budget = 12` (max entries before 12:00 UTC, refusal `Paced`) · `regime_vol_max = 1.5` (BTC `features.vol1h` above it refuses NEW entries, refusal `Regime`; BTC row/features absent → gate inactive + WARN once per UTC day; reviews/exits never gated) · `max_feature_age_s = 120` **hardened**: entry data age is now `max(snapshot_age, ws_feed_age)` so a REST-stamped `snapshot.ts` can no longer mask a dead mids stream (closes the R2 gap) · reviewer prompt gains live position context (`mark`, `unrealized_pnl`, `r_multiple` = uPnL ÷ |entry−sl|·size, `held_h`, estimated `fees_paid`) and **both** prompts carry the fee hurdle: round trip ≈15bp of notional, only open when the expected move clears ≥3× that cost. Gate chain order is now `gate_data_age` (pre) → `gate_entry` (FROZEN, untouched) → `gate_regime` → `gate_churn`; every refusal lands on the decision row as ` gate_refused:<Kind>`. User-locked values untouched (conviction_min 0.70, stop clamps 1.0/3.0, kill 12%, daily_cap 20, vlm floors, top_k, min_score).
10. **conviction_min = 0.75** (user-directed 2026-08-09 evening, was 0.70 from `b29d645`; commit e26f2c4): first real `/api/analytics` conviction buckets put the entire bleed in the bottom band — **0.70–0.75 = 16 closes, 38% wins, −$62.72** vs **0.75–0.80 = 4 closes, 75% wins, +$41.89**. Small sample acknowledged and accepted; the raise is a deliberate bet that the marginal 0.70–0.75 entries are noise, not edge. Changed in `traderd/traderd.toml` `[risk]` and pinned by `config::loads_traderd_toml_and_checks_key_fields`. The `/api/analytics` bucket floor stays **0.70** so pre-amendment closes (the evidence for this change) remain visible; nothing under 0.70 was ever executable. Next review point: ≥100 closes at the new threshold.

11. **Position size cut** (`margin_max` 50.0 → 25.0, 2026-08-09 night, user-approved): the mechanical edge is unproven and fee drag exceeds gross (net −71.30 on gross +15.80 vs fees 87.10 over the only obtainable 3.7-day tape; noise floor ~$90 sd per 77-close run makes sub-$180 differences unresolvable). Creates no edge — cuts burn rate while clean post-funding_z-fix data accumulates. Effective margin at conviction 0.75: $40 → $25 (−37.5%), notional $800 → $500 at 20×. Kill-floor runway roughly doubles. Revisit at ≥100 post-fix closes. All other user-locked values untouched.
12. **Regime gate deliberately inert** (2026-08-09, evidence-backed non-change): every threshold from the measured BTC vol1h percentiles was swept and none beat `off` — `daily_cap` binds first, so a regime refusal DISPLACES an entry to a later minute rather than removing it (entries stayed at exactly 80 in all six runs) and the replacements were worse. `regime_vol_max = 1.5` stands, documented as inert in `kestreld.toml`. No number is better than a number that reshuffles entries.

## Running system (localhost ops)

| Process | How it runs | Ports/IO |
|---|---|---|
| `kestreld` (release binary) | **systemd user unit** (R2): `systemctl --user enable --now kestreld` — install/ops in `kestreld/deploy/README.md`, unit `kestreld/deploy/kestreld.service` (`Restart=on-failure`, `RestartSec=5`, `KillSignal=SIGINT`, journal logging). Needs `loginctl enable-linger $USER` + `systemd=true` in `/etc/wsl.conf`. | API `127.0.0.1:7411`, log `journalctl --user -u kestreld -f`, DB `kestreld/kestreld.db` |
| `kestreld` **fallback only** (no systemd) | `setsid nohup ./kestreld/target/release/kestreld --config kestreld.toml` → `kestreld/smoke.log` — **unsupervised**: no restart on panic/crash, watchdog will alert but nobody restarts it | same ports, log `kestreld/smoke.log` |
| `tg_news_pipe.py` | **systemd user unit written, NOT cut over** (lead cuts over): unit `kestreld/deploy/tg-news-pipe.service` (`WorkingDirectory=%h/botta`, `Restart=on-failure`, `RestartSec=10`, `KillSignal=SIGINT`, `PYTHONUNBUFFERED=1`, journal, `SyslogIdentifier=tg-news-pipe`) — install/cutover in `kestreld/deploy/README.md`; supervision is the cure for the `setsid -f` silent-death gotcha. **Today it still runs under** `nohup uv run --with telethon --with httpx python tg_news_pipe.py & disown` — do not `enable --now` before killing that, or two pipes share one TG session. Cutover: `pkill -f '[t]g_news_pipe.py'` → confirm clear → `systemctl --user enable --now tg-news-pipe` | session `botta_news.session`, pushes `POST /ingest/news`; log after cutover `journalctl --user -u tg-news-pipe -f` |
| `dash` (Next dev) | `setsid nohup ./node_modules/.bin/next dev -H 127.0.0.1 -p 3474` (prod: `pnpm build && pnpm start`) | `127.0.0.1:3474` |
| stop | kestreld: `systemctl --user stop kestreld` (fallback: `kill -INT $(pgrep -x kestreld)`) · pipe **pre-cutover**: `pkill -f '[t]g_news_pipe.py'` (**bracket trick — never let pkill -f match your own cmdline**; matches both the `uv run` parent and its `python` child — verify both are gone) · pipe **post-cutover**: `systemctl --user stop tg-news-pipe` |

Log hygiene (`kestreld/deploy/README.md`): `kestreld/smoke.log` is nohup-era append-forever (52 KB, frozen since the systemd cutover — supervised daemons log to journald). `deploy/logrotate-botta.conf` = weekly · rotate 4 · compress · copytruncate, run via a systemd user timer (`logrotate` and `cronie` are both **not installed**; install before wiring). Journals are at 854.7 MB with stock `journald.conf` (default cap = 10% of fs / 4 GB) — `SystemMaxUse=200M` is documented, **not applied** (needs root); user-level equivalent is `journalctl --user --vacuum-size=200M`.

## Since then (2026-08-09 evening)

- **Analyst stack v2**: prompt JSON demand `abe1c0b` → `json_object` structured output `fbbbfd08` (json_schema verified INCOMPATIBLE) → client token cap REMOVED entirely `2c74966` (server default; 60s/call wall bound) → **hosted web_search self-retrieval ON** `bca9f0c` (queries logged in decisions.reason) → refusal-kind forensics `e4988fa` (empty_output/no_json/incomplete_max_tokens/http_err/timeout + sanitized excerpt in decisions.reason). Endpoint verified live for all of these.
- **the caller group alpha piped**: `[news] tg_signal_groups` — caller1-only forwarded as `tg:caller_group` `2f38961` (chatter analyzed: 55/120 msgs, 16 structured calls; everyone else noise).
- **HIP-3 perps trade 24/7** (was wrongly US-RTH-gated): evidence = weekend allMids streaming + caller trading SPCX on Saturday; `market_hours` now always open `3b0c37e`.
- Pipe daemonization gotcha: `setsid -f nohup` silently kills tg pipe within minutes; use `nohup uv run ... & disown`. **Cure written (pending cutover):** `traderd/deploy/tg-news-pipe.service` supervises it — `Restart=on-failure` brings the pipe back instead of it going quiet.

## Stop-loss bleed fix (2026-08-09 night)

- **stop_pct = clamp(1.5·vol1h, 1.0, 3.0)** (floor 0.6→1.0, ceiling 2.5→3.0; TP stays 2R) `53a5a64` — day-2 tape: 8 SL = −$110 at 1.2–1.9% stop bands in 3–5% noise assets. Analyst stop override floor aligned to 1.0; review-loop MoveStop tighten clamp kept 0.4 (profit-locking direction). Judgment window: Mon–Wed.
- Everything else held as control variables: min_score 1.8 (untouched since `b29d645`). **conviction_min is no longer a control** — raised 0.70 → 0.75 the same evening (amendment #10), so the Mon–Wed stop-fix window carries two changes at once; attribute results accordingly.

## Dash correctness (2026-08-09 night)

- **Refusal-rate parsing bug** `99cb9ef` — dash counted any reason containing the word "refused" (i.e., ALL of them → fake 100%). Now parses exact `refused:true`; intel renders `searched:"…"` web_search queries as 🔍 chips. Real refusal rate at the time: 0/50 (0%) under analyst stack v2.
- Trades `realized_pnl = 0` on `open` rows is by design (PnL realizes at close; opens carry fee only) — display of `—` for opens is a cosmetic follow-up, not data.

## Open follow-ups (non-blocking, pick from top)

0b-RESOLVED. **funding_z ring bug — FIXED `d1f03a5`** (was: 7d seed evicted by ~9s drift within 30min). Ring is now hour-bucketed: same-hour updates overwrite the newest slot, hour rollover appends; callers thread each sample's own timestamp. LIVE before/after (74→75 markets): |z|>1 36→28, and the tail became REAL — xyz equity perps read |z|≈7-8 on a genuine dislocation (xyz:TSLA funding −1.040e-04 vs its 7d mean +5.19e-06, ~20× magnitude, opposite sign) that the polluted ring had reported as noise. The screener was blind to it. WATCH: 24% of cached markets have near-degenerate funding variance (sd/mean <5%), where z explodes as sd→0 — a variance floor / min-sample guard is a warranted robustness follow-up, and 20 markets currently exceed the 2.5 side-flip threshold.
0e. ~~funding_z ring bug (LIVE, found by backtest parity)~~: the 7-day boot seed is evicted within ~30min — assetCtx funding drifts every ~9s and the dup-guard only collapses exact repeats, so the 200-slot ring holds intra-hour drift. Live BTC z −0.06 vs true settled −1.10; screener weights |funding_z| at 1.0 → nominations are driven by a broken statistic. Fix: push to ring only on settled hourly change (or time-aware ring).
0c. **Regime gate is dead as configured**: 3-day full-universe replay shows BTC vol1h peaked 0.079% vs regime_vol_max 1.5 — the threshold sits ~19× above the realized distribution and can never fire. Recalibrate from the distribution (~0.15–0.2) or make it percentile-based. — ✅ **ANSWERED, and the answer is "don't": see 0e.** (The suggested ~0.15–0.2 would also never fire: it is still 2× the measured max. Every threshold that DOES fire lost.)
0d. **Backtest findings (3d window, mechanical replay)**: fee drag is 83% of losses (gross −13.11, fees −62.67 best case); stop floor 1.0 validated on independent data (0.6 → −124.84 noise stops, 1.4 → −148.02 far targets); cooldown_after_sl 120 validated (−75.78 vs −91.31); every run capacity-bound at daily_cap 20 — caps dominate outcomes. Full tables: docs/backtests/2026-08-09-first-sweeps/.
0a. **Veto-engine verdict watch (NEW, from complete counterfactual data)**: with all 19 veto counterfactuals computed, the edge FLIPPED — vetoes net-cost $6.53 vs letting brackets run (+$11.74 actual vs +$18.28 bracket). Early 3-sample "+$21 edge" was noise. If this holds at ~50 computed, the 15-min review loop's veto authority should be narrowed (e.g. veto only on adverse news, not price action). Watch on /intel Analytics.
0e. **Regime recalibration — swept from the measured distribution, NO threshold earned a change (2026-08-09, window 2026-08-06 00:00 .. 2026-08-09 21:49 UTC, 5373 bars × 271 markets, `docs/backtests/2026-08-10-strategy-sweeps/regime/`)**: thresholds taken straight off `backtest stats`' BTC vol1h percentiles (p75 0.0277 / p90 0.0407 / p95 0.0509 / p99 0.0695, n = 5364 minutes) against `off` as control. Result — `off` ≡ 1.5 ≡ **p99** ≡ **p95** (net **−71.30**, all four byte-identical, **0 Regime refusals**: on this tape no nominee ever survived `gate_entry` inside a >p95 BTC minute); **p90** 566 refusals → **−71.38**; **p75** 2540 refusals → **−150.48** (win 35.1% → 29.1%). The mechanism is capacity, not regime: `daily_cap` 20 binds first, so a regime refusal never REMOVES a trade — entries stay at exactly **80 = 20 × 4 UTC days in every single run** — it only DISPLACES one to a later minute, and the replacements were worse. **Applied: nothing.** `regime_vol_max` stays **1.5** (user-locked amendment #9) with an evidence comment in `kestreld.toml` recording that it is inert BY DECISION; no amendment #11 was written because no number won. Revisit only on a tape that actually contains a BTC vol regime — this one does not (max 0.0794%).

13. **External web retrieval replaces hosted muse web_search** (user-directed 2026-08-21, `74cdb13`): hosted `web_search` tool DELETED from analyst requests (`tools_web_search` knob removed entirely); decide/review prompts now carry a client-side `RETRIEVED WEB CONTEXT` block built from parallel Tavily (`topic=news days=1`, Bearer auth per news.rs pattern) + Exa searches — deduped by URL, ≤6 items, ≤1800 chars; per-market **15-min TTL cache** bounds API spend (None results cached too); both providers failing → section omitted, decide proceeds (ATR discipline); issued queries still land in decisions.reason as `searched:"q1","q2"`. Keys `TAVILY_API_KEY` + `EXA_API_KEY` in `.env`. Model stays muse-only (amendment #1 intact). Tests 330→331.

14. **Call-driven autonomy + analyst chat** (user-directed 2026-08-21, `99040fc` + `c295f2b`): (a) directional `tg:caller_group` posts (`is_directional_call` — entry/long/short + sl/tp markers; manage-chatter excluded) force-nominate their markets into the next screener tick (10-min expiry, ≤4/tick), bypassing ONLY min_score — every risk gate still binds; decision reasons carry `forced:caller_group`. (b) analyst failure streak exposed as `/api/health.analyst_failure_streak`; ≥8 consecutive transport failures → hourly-deduped TG alert `analyst_down`, one `analyst_up` on recovery (closes the 8-day silent-death gap). (c) **Analyst chat**: `POST /api/analyst/chat` (local-only) — muse with portfolio/decisions/news context; trade intent = fenced JSON in reply, routed through the SAME gate chain + sizing + paper fills (conviction clamped [0.70,0.95], conviction_min 0.75 still binds); history at `GET /api/analyst/chat/history`. (d) **Full call transparency**: every decide/review/chat call recorded (`analyst_calls`: prompt, raw response, outcome kind, parsed JSON, latency) via migration 0004; surfaced at `GET /api/analyst/calls` and dash `/analyst` (chat panel + why-it-traded feed, IST timestamps). Tests 336→338. First live evidence: chat round-trip with real portfolio context; BOME open-intents at conviction 0.62/0.64 correctly refused by the locked 0.75 rail.

15. **Kill switch opt-in, default OFF** (user-directed 2026-08-21 ~13:37 IST, `f33a85d`): `[risk] kill_enabled = false` — paper equity collection runs uninterrupted through drawdowns; no kill halts, no kill alerts. `kill_threshold_pct` knob retained; setting `kill_enabled = true` restores byte-identical old behavior. Rationale: PAPER-only system, uninterrupted data matters more than simulated drawdown halts.

16. **Streaming analyst chat** (user-directed 2026-08-21 ~13:40–13:49 IST, `e000ab4` + dash `f24321f`, stream-parse fix `5ae08f3`): `POST /api/analyst/chat` now speaks the **AI SDK UI Message Stream Protocol** (SSE: start → text-start → text-delta… → text-end → optional data-action → finish → [DONE]); muse called with `stream:true`, deltas relayed token-by-token. CRITICAL lesson pinned: muse serves the **Responses API** (`{base}/responses`) whose stream events are `response.output_text.delta` with top-level `delta` — NOT chat-completions `choices[].delta.content`; parsing the wrong shape yields silent empty replies. Trade intents remain fenced JSON routed through the same gate chain. Dash `/analyst` rebuilt on `@ai-sdk/react` useChat + DefaultChatTransport + **Streamdown** markdown renderer; action cards show EXECUTED/REFUSED with gate reasons; IST timestamps throughout. Verified end-to-end live (curl SSE frames + Playwright round-trip, zero console errors).

17. **Books stream sharded** (`93de8ad` + reshard follow-up `7555e98`): the exact-60s reconnect cycle (follow-up #12) is FIXED — root cause consistent with HL killing burst-subscribed connections (~140 l2Book subs on one socket); now ≤50-market shards with staggered subscribes (10 frames/150ms). Live evidence: **0 reconnects in a 25-min window** (was ~25). Dynamic resharding tracks hourly universe refreshes (new listings subscribe on the shard with capacity, overflow spawns a shard; removals unsubscribe — HL ws supports explicit unsubscribe frames).

18. **Multi-analyst ARENA** (user-directed 2026-08-22 ~02:30–04:10 IST, `db3bf69`+`42d1117`+`ad08162`, dash `5ada67d`): NINE free OpenCode Zen models (`x-preview-f-free` "Ox Alpha", muse-spark-contributor-free, big-pickle, deepseek-v4-flash-free, hy3-free, laguna-s-2.1-free, mimo-v2.5-free, nemotron-3-ultra-free, nemotron-3.5-lightning-free) each trade the shared $1000 paper book INDEPENDENTLY — own position book, own gate state (dup/cooldown/per-analyst caps; cross-analyst same-market ALLOWED by design), reviews route to the owning model, chat answers via `chat_model`. Zen needs identity headers (`x-opencode-*` + opencode UA) or 403 even with a valid key — shipped as `[analyst] headers`. New `api_style = "chat"|"responses"` per model (free models are chat-completions except muse-free); streaming captures `reasoning_content` separately from content deltas. `[risk] global_max_concurrent = 12` caps combined exposure; daily entry budget enforced GLOBALLY (`GlobalDailyCap`) after live-found bug showed per-analyst counters leaking into /api/gates. **conviction_min 0.75 → 0.50** — user-directed: neutral floor only, the arena measures each model's calibration (leaderboard now carries `avg_entry_conviction`; full roster shows even at zero trades). Amendment #1's muse-only rule is superseded by this user directive; muse remains in the roster as one competitor. Leaderboard: `GET /api/analyst/leaderboard`.
19. **First arena results** (live, first hours): user's chat-opened SOL long TP'd **+$11.38 net** (90.58 → 92.78, trigger engine, 02:53 IST); equity $882.40 → $893.77. First fan-out put FIVE models long ZORA simultaneously (consensus trade) — 12 positions across 6 models within the hour, aggregate uPnL ≈ +$25. Chat trades now persist to `decisions` with full forensics (`eab032d`).
0f. **Fee/capacity sweeps — the strategy earns ~18¢ of gross per $1 of fee, and NO swept knob fixes it (2026-08-09, same window, `docs/backtests/2026-08-10-strategy-sweeps/{daily-cap,margin-size,min-score,tp-mult,morning-budget}/`)**: baseline (config as-is) = net **−71.30**, gross **+15.80**, fees **87.10** on **80 entries / 77 closes / 35.1% win / payoff 1.55 / maxDD 102.98** — turnover $116 138 at a flat 15bp round trip, i.e. **$1.089 of fee per entry against $0.198 of gross**, so fees are **122%** of the loss (B3's 83% got worse once gross went positive). Answers: (a) **`daily_cap` {20,12,8,5} — trading less loses MORE** (−71.30 / −123.56 / −105.45 / −95.96); fees fall exactly linearly ($1.09–1.11/entry) but gross collapses +15.80 → −70…−74 at every cut, because the cap truncates each UTC day at its FIRST N signals and those are the bad ones. (b) **size is exactly neutral on edge**: at a fixed trade set, gross AND fees both scale with margin — margin 20 vs 40 give identical gross/fee ratios to 3 decimals (0.181, 0.207, −2.380, −3.334); above that the 12% kill switch latches (margin 80 → 1 kill day, 160 → 4 kill days, entries 80 → 27) and everything gets worse. At **constant total risk** (margin×daily_cap = 800) fewer-larger is strictly worse than more-smaller: −56.39 (20×40) / −71.30 (40×20) / −296.33 (80×10) / −383.85 (160×5) at a near-constant ~$71–89 fee bill. (c) **selectivity does not beat frequency**: `min_score` 1.8 ≡ 2.5 byte-identical (the caps, not the floor, pick the trades), 3.5 → −141.50. (d) **wider targets do not clear the hurdle**: `tp_mult` 2.0 −71.30 / 2.5 −71.61 (payoff 1.55 → 1.79, maxDD 102.98 → 95.72) / 3.0 −111.28 (win 35.1% → 28.2%). **Recommended (NOT applied, lead's call): halve position size while the edge is unproven** — `[sizing] margin_max = 50.0 → 25.0` (conviction-0.75 margin $40 → $25), the only change whose effect is arithmetic rather than selection-dependent: net −71.30 → ≈−45 and maxDD 102.98 → ≈64 at an unchanged gross/fee ratio. It shrinks the loss; it does not create an edge. **NOISE FLOOR — read before acting on any of this:** per-close net sd is $10.28 (35.1% × +13.09 / 64.9% × −8.46), so the sd of a 77-close run's total is **≈$90**. Every `daily_cap`-20 run above pays the same ~$85–87 of fees on the same 77–80 entries; the whole −59 → −243 spread is WHICH 80. A `morning_entry_budget` probe {0,3,6,12,20} → −59.23 / −109.84 / −242.58 / −71.30 / −92.40 is non-monotone by $183, which is the falsification: at n≈80 trades over 3.7 days, displacement luck swamps every knob tested. Nothing under ~2 sd (~$180) of net difference is a finding.
0. **Conviction calibration**: ✅ acted on — conviction_min raised **0.70 → 0.75** (amendment #10, 2026-08-09 evening) after the /api/analytics buckets put all the bleed in the 0.70–0.75 band (16 closes, 38%, −$62.72) against 0.75–0.80 (4 closes, 75%, +$41.89). Next review point: **≥100 closes at the new threshold** — compare 0.75–0.80 vs 0.80+ then, and only revisit the floor again on that larger sample.

1. ~~Maintenance batch ece2a79/3cf8b5a/5d81c5e~~ ✅ · ~~snapshot gaps~~ ✅ `55cce9b` + zero-mark guards `ee441ee` · ~~partial_close depth~~ ✅ `ece2a79` (2026-08-09).
2. Doc note: 30s ctx poll intentionally also grows `markets_tracked` (new listings passing vlm filter).
3. ~~`Decimal::ZERO`~~ ✅ `3cf8b5a`.
4. ~~Config-parse regression test~~ ✅ exists (`config::loads_traderd_toml_and_checks_key_fields` — caught the `b29d645` vlm raise; updated to 2M/500k expectations).
5. ~~Tavily per-market cooldown~~ ✅ `5d81c5e` (15 min).
6. RSS poll interval currently hard-coded 300s (news.rs) — make config-driven 60s if speed demanded.
7. Optional 11th channel: `@cryptoquant_alert` (verify first — preview page showed boilerplate only, may be inactive/private).
8. VPS deploy (user picked plan: **Cloud VPS 4, 4vCPU/8GB/100GB @ ~$6.2/mo, region nearest user**; systemd ×3, SSH tunnels 7411/3474), then kill local processes.
9. Rotate TG bot token (transcript exposure) + then update only `.env`.
10. Dash trades table: render `—` instead of `$0.00` realized_pnl for `open` rows (cosmetic).
11. Dash: 10 pre-existing react-hooks lint errors (`pnpm build` unaffected) — cleanup batch.
12. **books ws reconnects every ~60s, exactly, since at least the 2026-08-13 boot** (`ws stream ended stream="books"` → reconnect ~every 60s like clockwork). External single-subscription probe from the same host is stable >90s with frames flowing, so it's not the network — suspect HL-side reaction to kestreld's ~140-subscription burst per books connection (per-market l2Book) or a per-connection subscription cap. Impact contained: mids/ctxs healthy, entries/exits fill via the flat 2bp/5bp fallback instead of impact VWAP. Follow-up: stagger/batch l2Book subscriptions, or move books to its own connection pool.
13. **Ops recovery 2026-08-21** (all resolved): muse key dead since the 08-13 boot — every analyst call `http_err` for 8 days ⇒ zero entries while feed stayed green; new key live 06:39Z. tg-news-pipe start-limit-latched since 08-20 04:45Z (Telegram ConnectionError ×6); reset-failed + start 06:42Z, 10 channels + signal group listening. dash down post-reboot; `bun run start` on 3474 06:42Z. Lesson: analyst failures are SILENT for trading (decisions just stop) — consider an operator alert on N consecutive analyst failure episodes. ✅ alert shipped same day (amendment #14b).
14. **Dash ops gotcha (hit twice)**: after ANY `pnpm build`, the running `next start` server MUST be restarted — stale chunk manifests make every static asset 500 and silently break hydration (buttons never enable). Restart = kill exact PID on 3474 → `bun run start` fresh.
15. **Afternoon build-out 2026-08-21 (autonomous, ~12:00–14:10 IST, user asleep)**: books sharding deployed + verified (0 reconnects/25min); dynamic reshard for universe refreshes; kill switch default-off (amendment #15); chat streaming end-to-end incl. Responses-API stream-parse fix (amendment #16); dash /analyst rebuilt with Streamdown markdown + useChat streaming, verified via Playwright round-trip. Gates at every step: cargo test 348/348 green, clippy clean, pnpm build green. Commits: `93de8ad 7555e98 f33a85d e000ab4 5ae08f3 f24321f`. Evening follow-ups ~18:25 IST: skip-recheck cooldown `6c19c04` (737→~150 decide calls/hr expected), dash open-row em-dash `d0ff487`.

## Agent working rules (inherited opencode.json protocol)

Leader (orchestrator) never writes app code — muse subagents (`meta/muse-spark-1.2-contributor`) implement per plan; leader independently re-runs gates, inspects diffs for scope drift, and alone deploys/restarts. One worker per write scope at a time. **Paper-only invariant belongs to every role.** Never trust a claimed pass without rerunning it.

20. **Manage-only halt** (user-directed 2026-08-22 ~16:40 IST, `1921ca3`+`9d57ca9`): `[risk] entries_enabled=false` refuses all new entries (`EntriesHalted`, incl. force-noms + chat opens) while SL/TP/time-stop triggers, review loop, closes and equity snapshots stay live; `[analyst] enabled=false` quiesces ALL LLM activity (no decide fan-out, no retrieval, no reviews, chat replies locally without upstream). `/api/gates` exposes both flags. Context: arena first-18h realized per leaderboard — big-pickle +26.26 (62.5%), x-preview-f-free +26.00, laguna +16.26, mimo −45.37 (0/7), muse-free+deepseek never traded; equity $1000 → $838.63. Caps removed same day: daily_cap=0, max_concurrent=0, global_max_concurrent=0 (`042adb1`, `11ca9e6`); top_k 6→15 (`2c67795`); ws liveness watchdog ends silent-death outages (`5f8e999`).